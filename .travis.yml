language: python
python:
  - "2.7"
env:
  global:
    - PIP_DOWNLOAD_CACHE=$HOME/.pip_cache
  matrix:
    - TOX_ENV=py27-django111
    - TOX_ENV=py27-django20
    - TOX_ENV=py27-django21
    - TOX_ENV=py35-django111
    - TOX_ENV=py35-django20
    - TOX_ENV=py35-django21
    - TOX_ENV=py37-django111
    - TOX_ENV=py37-django21
    - TOX_ENV=flake8
cache:
  directories:
    - $HOME/.pip-cache/
install:
  - "travis_retry pip install setuptools --upgrade"
  - "pip install tox"
script:
  - tox -e $TOX_ENV
after_script:
  - cat .tox/$TOX_ENV/log/*.log
